#pragma once
#include "struct.h"
#define MAXLOGIN 20
#define LINE 128
#define MAX 200

char id[MAXLOGIN];
char pw[MAXLOGIN];
char path[LINE];
char pathlist[LINE];
char pathfriends[LINE];
char pathteams[LINE];

Todo list = { 0 };
Todo todos[200] = { 0 };
Todo todo = { 0 };